info:
  name: random
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/posts/random"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
