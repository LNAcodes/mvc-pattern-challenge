info:
  name: SQL DB Test
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:3000/api/posts/latest
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
