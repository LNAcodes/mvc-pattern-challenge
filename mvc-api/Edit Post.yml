info:
  name: Edit Post
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3000/admin/posts/:slug/edit
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
